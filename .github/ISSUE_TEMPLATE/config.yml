blank_issues_enabled: true
contact_links:
  - name: Migration question
    url: https://github.com/swey/vitest-expo/blob/main/MIGRATION.md
    about: The migration guide covers the patterns that need a change, with examples.
  - name: React Native engine issue
    url: https://github.com/danfry1/vitest-native/issues
    about: Problems in the React Native layer itself (transform, native modules, presets) belong upstream in vitest-native.
